services:
  classifier:
    build: .
    ports:
      - "8765:8765"
    environment:
      CLASSIFIER_MODE: heuristic
      CLASSIFY_REQUIRE_PANORAMA: "0"
      FLASK_PORT: "8765"
    volumes:
      - ./web_app/uploads:/app/web_app/uploads
