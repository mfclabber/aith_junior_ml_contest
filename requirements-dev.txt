# Разработка, MLOps и автономный агент (extras)
# Установка: pip install -r requirements-dev.txt

# Тесты и качество кода
pytest>=8.0
ruff>=0.4
black>=24.0
pre-commit>=3.6

# Конфигурация и трекинг экспериментов
hydra-core>=1.3
omegaconf>=2.3
tensorboard>=2.15
wandb>=0.16
mlflow>=2.10
dvc>=3.0
rich>=13.0

# Автономный агент (LangGraph)
langgraph>=0.2
langchain-core>=0.3
